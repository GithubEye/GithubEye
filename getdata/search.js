following_list = require('./following_list.js');
get_stars = require('./get_stars.js');
repo_list = require('./repo_list.js');

module.exports = {
	search : function(user , desc , extra_res)
	{
		userlist = following_list.following_list(user , )
	}
};